{
  "name": "dashboard",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",

  "scripts": {
    "server": "nodemon app.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node app.js",
    "heroku-postbuild": "cd dashboardui && npm install --only=dev && npm install && npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "config": "^3.3.6",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "fusioncharts": "^3.16.0",
    "mongoose": "^5.12.5",
    "react-fusioncharts": "^3.1.2"
  },
  "devDependencies": {
    "nodemon": "^2.0.7"
  },
  "engines": {
    "node" : "10.19.0"
  }
}
